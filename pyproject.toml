[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["compositio"]

[tool.black]
line-length = 120

[project]
name = "pycompositio"
version = "0.4.2"
authors = [
  { name="Victor Adan", email="vctradn@gmail.com" },
]
description = "Functional programming in Python"
requires-python = ">=3.12"
license = "MIT"
classifiers = [ ]
dependencies = [ ]

[project.urls]
Homepage = "https://github.com/victoradan/pycompositio"
